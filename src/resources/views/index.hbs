<div class="row">

  <div class="col-lg-4.col-sm-6">
    <div class="card" style="width: 18rem;">
      <a href="/course/{{this.slug}}"><img
          class="card-img-top"
          src="{{this.image}}"
          alt="Card image cap"
        /></a>
      <div class="card-body">
        <h5 class="card-title">{{this.name}}</h5>
        <p class="card-text">{{this.description}}</p>
        <a href="/course/{{this.slug}}" class="btn btn-primary">Xem chi tiết</a>
      </div>
    </div>
  </div>

</div>